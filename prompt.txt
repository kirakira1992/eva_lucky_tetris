请给我一个最小可运行的俄罗斯方块项目骨架：index.html/style.css/main.js。
只需要显示一个居中的 canvas（宽 300 高 600），旁边写按键说明。
暂时不要实现任何游戏逻辑，但 main.js 要能画出 10x20 的空网格线。
代码要完整可复制粘贴，双击 index.html 就能运行。

Round 0（骨架 + 空网格）

Prompt 0.1（新建项目骨架）

我不会编程。请给我一个最小可运行的项目：index.html / style.css / main.js。
约束：只用原生 HTML/CSS/JS，不用框架、不用 npm。
需求：页面居中显示 canvas（300x600），右侧显示按键说明。
main.js 只需要画出 10x20 的空网格线（格子大小 30px），不要任何游戏逻辑。
请输出：文件结构 + 三个文件的完整代码 + 我如何运行。

Prompt 0.2（让画面更像游戏界面，可选）

在不改动核心结构的前提下，美化 UI：黑色背景的游戏区、浅色网格线、右侧信息面板（Score/Level/Lines 的占位）。
仍然不要游戏逻辑。请给出需要改动的代码（最好按文件分段）。

Round 1（能下落、能移动：先做 1 种方块）

Prompt 1.1（实现单方块下落 + 左右移动）

现在在现有代码基础上实现最小可玩版本：

网格 10x20，方块用 1 种形状（先用 O 方块 2x2 就行）。

方块从顶部生成，每 500ms 自动下落一格。

键盘：← → 左右移动；↓ 加速下落（按住更快）。

碰撞：不能出边界，不能穿过底部。到底后先“停止”即可（先不生成新方块也可以）。
约束：不要重写项目，只修改 main.js（如必须改 HTML/CSS 请说明原因且最小改动）。
请给我：main.js 的完整替换版本 + 运行/测试步骤。

Prompt 1.2（落地后生成新方块 + 堆叠）

在你刚实现的版本上继续：

方块落地后要“锁定”到 board（变成固定方块）

然后自动生成下一块 O 方块从顶部继续掉落

如果新方块一生成就发生碰撞，判定 Game Over（屏幕显示 Game Over 文案，停止循环）
请只改 main.js，并标注你新增/修改了哪些函数。

Prompt 1.3（硬降）

加一个“硬降”：按空格 Space，当前方块直接落到能落的最低位置并立刻锁定，然后生成下一块。
仍然只改 main.js，尽量小改动，并告诉我怎么验证。

Round 2（完整核心：7 方块 + 旋转 + 消行 + 计分）

Prompt 2.1（加入 7 种方块 + 随机生成）

现在请把方块从单一 O 扩展为标准 7 种 Tetromino（I, O, T, S, Z, J, L），随机生成。

仍然使用 10x20 board

仍然支持：自动下落、左右移动、软降(↓)、硬降(空格)、落地锁定、Game Over
暂时先不做旋转和消行。
只改 main.js，给我完整替换版本 + 测试清单（每个形状如何确认生成正确）。

Prompt 2.2（旋转）

在当前版本加入旋转：

↑ 或 X 键旋转顺时针

遇到墙/碰撞时：先实现最简单处理——旋转后如果非法就取消旋转（不需要复杂 wall kick）

请确保 I 方块旋转也正常
只改 main.js，并说明你用的形状表示法（矩阵/坐标）和旋转实现方式。

Prompt 2.3（消行 + 计分 + Lines）

加入消行和计分：

任意一行满了就清掉，上面的整体下落

Lines 统计累计消除的行数

Score 规则：消 1 行=100，2 行=300，3 行=500，4 行=800（一次下落锁定后结算）

右侧面板显示 Score/Lines（如果你之前做的是占位，现在把它更新为真实值）
如需改 index.html 显示数值可以最小改动。请给我改动后的完整文件代码（只给改动过的文件）。

Prompt 2.4（Level 和加速）

加入 Level 和速度变化：

Level 从 1 开始

每消除 10 行升 1 级

下落间隔随 Level 缩短（例如：500ms * 0.9^(level-1)，但不要低于 80ms）

UI 显示 Level
只做必要改动，给我怎么验证 Level 升级与速度变化。

Round 3（更像真正 Tetris：Next / Hold / 暂停 / 手感）

Prompt 3.1（Next 预览）

做 Next 预览：

右侧增加一个小 canvas（或在同一 canvas 旁边画一个小区域也行）显示下一块

逻辑：采用“当前块 + nextQueue”的方式，锁定后把 next 变 current，再随机生成新的 next
约束：尽量少改结构。给出你改动的文件及完整代码。

Prompt 3.2（Hold 功能）

加入 Hold：按 C 键

如果 hold 为空：把当前块放入 hold，当前块改为 next

如果 hold 不为空：交换 current 和 hold

每次生成新块后，只允许 hold 一次（锁定后重置可 hold 状态）
UI 显示 hold 区域（简单即可）。请提供完整改动代码和测试步骤。

Prompt 3.3（暂停 + 重开）

加入：

P 键暂停/继续（暂停时停止下落与按键移动）

R 键重新开始（清空 board、重置分数、重新生成方块）

Game Over 状态下按 R 也能重开
请尽量小改动，给我验证 checklist。

Prompt 3.4（手感微调：DAS/ARR 可选）

我想让左右移动更顺滑：

按住 ←/→：先立刻移动 1 格，然后延迟 150ms 开始连续移动

连续移动间隔 50ms
只要实现基础版本，不追求完美。请说明你如何处理 keydown/keyup。


round 4:我已经有一个基础的俄罗斯方块（HTML + CSS + JS，canvas实现）。

现在我要把它改造成一个 Eva 专属的可爱治愈版本，整体风格参考：
- 奶油色背景
- 手绘卡通风
- 圆角元素
- 粉色 + 米色 + 柔和橙色配色
- 类似日系可爱手机主题 UI

请帮我做以下改造（保持代码结构清晰，不要推翻重写，尽量在现有基础上修改）：

========================
🎨 一、整体UI风格升级
========================

1. 背景改为柔和奶油色（#F8F3E8）
2. 游戏画布增加圆角（border-radius: 20px）
3. 方块颜色改为柔和粉色/杏色/奶茶色系
4. 右侧面板改为卡片风格（带阴影）
5. 字体风格可爱圆润（使用系统圆角字体即可）
6. 所有UI元素避免锐利直角

整体视觉要有：
- 治愈
- 手绘
- 少女感
- 轻松

========================
🐶 二、消行特效（核心亮点）
========================

当一行被消除时：

1. 在游戏中央弹出一个可爱狗狗图像（可以用：
   - 简单的 canvas 绘制卡通狗
   - 或 emoji 🐶
   - 或用简单SVG画一个卡通小狗）

2. 狗狗出现时：
   - 稍微放大缩小动画（scale动画 0.8 -> 1.1 -> 1）
   - 显示 600ms 后消失

3. 同时播放“烟花特效”：
   - 使用 canvas 粒子效果
   - 或简单多彩圆点向外扩散
   - 不需要复杂物理，只要视觉有“爆炸散开”效果

4. 右侧短暂显示文字：
   “Lucky is proud of you 🐶✨”

========================
🌸 三、游戏主题名
========================

右侧标题改为：

"EVA Lucky Tetris"

Score 改名为：
"Joy Points"

Lines 改名为：
"Happy Lines"

========================
🎉 四、Game Over 文案修改
========================

当游戏结束时：

1. 屏幕中央显示：

“关注 Eva，永远留在牌桌上 ♡”

2. 字体大一点，居中，带轻微弹跳动画
3. 下方加一个按钮：
   “重新开始”

按钮风格：
- 粉色圆角按钮
- 悬停微微放大

========================
💡 五、技术要求
========================

- 不要使用任何外部库
- 仍然使用原生 JS + canvas
- 动画可以用 requestAnimationFrame
- 所有新增代码请分段解释
- 如果需要新增变量，请命名清晰
- 保持主逻辑（碰撞、消行）不变

最后请输出：
1. 需要修改的文件
2. 每个文件完整代码
3. 新增功能说明
4. 如何测试消行特效和Game Over

目标不是复杂，而是可爱、治愈、有粉丝记忆点。